const NumberCourse = (props) => {
    return (

        <div>
            <h1> Phone Number : {props.number}</h1>
            <h1> Course : {props.course}</h1>
        </div>
    )
}

export default NumberCourse